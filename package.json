{
  "name": "electron-hook-template",
  "version": "1.0.0",
  "description": "Save all fetch resources to local caches. if local file exists override it.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint -c .eslintrc --ext .ts ./src",
    "start": "yarn run build && electron ./dist/main.js"
  },
  "repository": "https://github.com/dev-templates/electron-hook-template",
  "keywords": [
    "electron",
    "hook",
    "template",
    "nodejs",
    "typescript",
    "eslint"
  ],
  "author": "leaker",
  "license": "SEE LICENSE IN <LICENSE>",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.34.0",
    "@typescript-eslint/parser": "^5.33.1",
    "electron": "^20.0.3",
    "eslint": "^8.22.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "winston": "^3.8.1",
    "winston-transport": "^4.5.0"
  }
}
